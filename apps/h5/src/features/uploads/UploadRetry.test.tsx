import { render, screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { UploadStep } from './UploadStep';
test('offers retry after upload failure', async () => { const user = userEvent.setup(); const upload = vi.fn().mockRejectedValueOnce(new Error('failed')).mockResolvedValueOnce({ fileId: 'ok' }); render(<UploadStep requirements={['Identity']} onUpload={upload} />); const input = screen.getByLabelText('上传Identity'); await user.upload(input, new File(['pdf'], 'identity.pdf', { type: 'application/pdf' })); const retry = await screen.findByRole('button', { name: '重试' }); await user.click(retry); expect(upload).toHaveBeenCalledTimes(2); });
