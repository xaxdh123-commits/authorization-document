import { useState } from 'react';
export function CaseCreatePage() { const [materials, setMaterials] = useState(['']); return <main><h1>Create case</h1><label>Customer<input aria-label="Customer" /></label><label>Contact<input aria-label="Contact" /></label><label>Template<select aria-label="Template"><option>Default authorization</option></select></label><label>Requirements<select multiple aria-label="Requirements"><option>Identity</option><option>Address</option></select></label><h2>Materials</h2>{materials.map((_, i) => <input key={i} aria-label={`Material ${i + 1}`} />)}<button type="button" onClick={() => setMaterials([...materials, ''])}>Add material</button></main>; }
