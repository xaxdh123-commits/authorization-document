import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { AuthProvider } from '../../auth/AuthProvider'; import { AppRouter } from '../../app/router';
test('缺少工作台读取能力时显示中文禁止访问页', async () => { render(<AuthProvider client={{ getSession: async () => ({ userId: 'u', roleKey: 'staff', abilities: [] }) }}><MemoryRouter initialEntries={['/dashboard']}><AppRouter /></MemoryRouter></AuthProvider>); expect(await screen.findByRole('heading', { name: '没有访问权限' })).toBeInTheDocument(); });
