export interface ReviewCommandClient { approve(): Promise<void>; reject?(): Promise<void>; }
export function ReviewDetailPage({ client }: { client?: ReviewCommandClient }) { return <main><h1>Review detail</h1><section><h2>Supplement differences</h2><p>Compare submitted and requested materials.</p></section><button type="button" onClick={() => void client?.approve()}>Approve item</button><button type="button" onClick={() => void client?.reject?.()}>Reject item</button><button type="button">Retry PDF</button></main>; }
