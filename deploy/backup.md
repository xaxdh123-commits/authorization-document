# 备份策略

- 数据库：每日使用独立只读备份凭证执行 PostgreSQL custom format 备份。
- 文件：每日增量复制本地磁盘或 NAS 挂载目录，并生成 SHA-256 清单。
- 保留：日备份 30 天、月备份 12 个月；备份目录不得位于业务文件根目录内。
- 安全：凭证仅通过目标服务器密钥/环境注入，日志不得输出连接串、Token 或证件内容。
- 目标：RPO 不超过 24 小时，RTO 不超过 8 小时。

本仓库只验证脚本参数和 dry-run；未执行真实备份。
