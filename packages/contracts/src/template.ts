import { z } from 'zod'; export const SignatureModeSchema=z.enum(['HANDWRITTEN','STAMP_UPLOAD']); export const SignatureSlotSchema=z.object({usage:z.string().min(1),page:z.number().int().positive(),anchor:z.string().min(1),required:z.boolean()}); export const TemplateVersionSchema=z.object({version:z.number().int().positive(),status:z.enum(['DRAFT','PUBLISHED','DISABLED']),ast:z.record(z.any()),signatureMode:SignatureModeSchema});
