import { z } from 'zod'; export const RequirementTypeSchema=z.enum(['TEXT','LONG_TEXT','SINGLE_SELECT','MULTI_SELECT','DATE','NUMBER','FILE','IMAGE']); export const RequirementStatusSchema=z.enum(['PENDING_INPUT','PENDING_REVIEW','APPROVED','REJECTED']); export const RequirementDefinitionSchema=z.object({key:z.string().regex(/^[a-z][a-z0-9_]*$/),label:z.string().min(1),type:RequirementTypeSchema,required:z.boolean().default(false)}); export type RequirementDefinition=z.infer<typeof RequirementDefinitionSchema>;
